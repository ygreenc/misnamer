import re
import ast
from setuptools import setup

setup(
    name='movieren',
    author='ygreenc',
    author_email='',
    version='0.1',
    url='',
    packages=['movieren'],
    description='Rename movie file'
)
